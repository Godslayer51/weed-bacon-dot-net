{
  "name": "weed-bacon-dot-net",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 8000"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
